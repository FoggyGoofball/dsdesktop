# Copilot Instructions

## Project Guidelines
- In DS keybinding UI, remapping semantics must be per single DS hardware button mapped to up to two keyboard keys; do not treat combined DS button presses as unique remap events. Overlapping presses should transmit each mapped key independently, and a single DS button may emit two keys simultaneously.